/**
 * هذا ملف مؤقت. بعد تطبيق ملفات supabase/migrations على مشروعك الفعلي على Supabase،
 * ولّد الأنواع الحقيقية تلقائياً بالأمر التالي (يستبدل هذا الملف بالكامل):
 *
 *   npx supabase login
 *   npx supabase link --project-ref yrzciifapkcbxyrwnebm
 *   npx supabase gen types typescript --linked --schema public > src/types/database.ts
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
