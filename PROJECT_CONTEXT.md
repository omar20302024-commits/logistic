# PROJECT_CONTEXT.md — ملف تسليم المشروع
### نظام إدارة السائقين والرحلات والتربات والرواتب والربحية

> آخر تحديث لهذا الملف: 2026-09-13
> هذا الملف مرجع شامل لأي شخص (أو أي جلسة عمل جديدة مع Claude) يحتاج يفهم المشروع من الصفر بدون الرجوع للمحادثة الأصلية.

---

## 1) فكرة النظام بالكامل

نظام ويب عربي (RTL بالكامل) لإدارة أسطول سائقين، الهدف الأساسي منه معرفة **الربحية الحقيقية**:
ربحية كل رحلة، ربحية كل سائق، ربحية كل شركة عميل، وربحية النشاط ككل — مش مجرد نظام حضور أو رواتب.

### المبدأ الأهم في كل النظام
فصل تام بين 4 مبالغ مختلفة لازم متتلخبطش مع بعض:
1. **سعر الرحلة** — اللي بناخده من الشركة العميلة (سري، للإدارة فقط).
2. **الترب** — المبلغ اللي يستحقه السائق عن الرحلة (يظهر له).
3. **الديزل** — مصروف مباشر على الرحلة.
4. **الراتب الشهري** — مصروف مستقل تماماً عن الترب، ما بيتخصمش من ربح أي رحلة بعينها.

```
ربح الرحلة (تشغيلي) = سعر الرحلة − الترب − الديزل   (لكل رحلة، بيتحسب تلقائياً)
صافي الربح (لفترة)  = إجمالي الربح التشغيلي − الرواتب − المصروفات الأخرى
```

### الفصل الأمني الحاسم
**كشف حساب داخلي** (للإدارة، فيه سعر الرحلة والربح) مختلف تماماً عن **كشوفات السائق** (آمنة، بدون أي بيانات سرية). ده متطبّق على مستوى قاعدة البيانات نفسها (دوال SQL منفصلة لا تُرجع أعمدة سعر الرحلة/الربح أصلاً)، مش بس إخفاء في الواجهة — عشان حتى لو حصل خطأ برمجي مستقبلي، البيانات السرية متوصلش للمتصفح من الأساس.

### أهم إضافة تصحيحية (مهمة جداً لفهم الكود)
**سعر الموقع الإضافي للعميل ≠ ترب السائق للموقع الإضافي.** كل موقع تحميل/تنزيل إضافي (غير الأول) بيتحاسب على العميل بالمبلغ المكتوب بالكامل (يدخل في سعر الرحلة)، لكن ترب السائق عن المواقع الإضافية بياخد من **معدل ثابت خاص بكل سائق** (`extra_stop_rate`) × عدد المواقع الإضافية — مش من المبلغ المكتوب للعميل. كل سائق له معدله، وكل عميل سعره، منفصلين تماماً (migration 0011).

---

## 2) الملفات الموجودة ووظيفة كل ملف

### مستندات مرجعية (لا تُنفَّذ، للفهم فقط)
| الملف | الوظيفة |
|---|---|
| `docs/01-architecture.md` | العمارة التقنية الكاملة، الـ Stack، هيكل المجلدات |
| `docs/03-erd.md` | مخطط العلاقات بين الجداول (Mermaid ERD) |
| `docs/04-business-rules.md` | كل المعادلات المالية وقواعد العمل (بعضها قديم شوية، هذا الملف هو الأحدث) |
| `PROJECT_CONTEXT.md` | **هذا الملف** — ملف التسليم الشامل |

### قاعدة البيانات (`supabase/migrations/`) — بالترتيب، كلهم لازم يتشغّلوا بالتسلسل
| الملف | يضيف/يعدّل |
|---|---|
| `0001_init_schema.sql` | كل الجداول الأساسية، الأنواع (enums)، الفهارس، trigger مزامنة سعر الرحلة من مواقعها |
| `0002_rls_policies.sql` | تفعيل RLS على كل جدول + سياسات صلاحيات الـ admin |
| `0003_views_and_functions.sql` | Views (`v_trips_full`, `v_trip_location_totals`) + دوال كشف حساب السائق والشركة والتقرير المالي |
| `0004_dashboard_analytics.sql` | دوال لوحة التحكم (نظرة عامة، اتجاهات شهرية، أعلى السائقين/الشركات ربحية) |
| `0005_trips_and_salaries_support.sql` | ترقيم الرحلات التلقائي + `v_salary_statements` (كشف راتب محسوب) |
| `0006_driver_public_summary.sql` | دالة ملخص آمنة لكشف السائق العام (بدون بيانات سرية) |
| `0007_reports_functions.sql` | دوال تقارير السائقين/الشركات/التربات/ربحية الرحلات |
| `0008_rentals_custody_vendor.sql` | العهدة (حساب جاري)، تصنيف السائق (داخلي/مورد)، عقود الإيجار الشهري + وحدات السكن |
| `0009_multi_stop_pay_and_cleanup.sql` | حذف "مؤقت/مثبت"، تربة أساسية لكل رحلة، تربة افتراضية لكل سائق (نسخة أولى، تم تصحيحها لاحقاً في 0011) |
| `0010_driver_route_rates.sql` | ترب حسب خط السير (مدينة تحميل → مدينة تنزيل) لكل سائق + `extra_stop_rate` |
| `0011_fix_extra_stop_billing.sql` | **الأهم**: تصحيح فصل سعر الموقع الإضافي (للعميل) عن ترب الموقع الإضافي (للسائق، معدل ثابت) |
| `0012_custody_entry_classification.sql` | تصنيف قيود العهدة (`reason` + `expense_category` + `trip_id`)، ودمج مصروفات السائق في `fn_financial_summary` |
| `0013_driver_settlements.sql` | تصفية التربات: `driver_settlements` + وسم الرحلات/العهدة/السلف بسند، ودلو `settle_against` للسلف والخصومات |
| `0014_report_reviewer.sql` | `settings.reviewed_by` — اسم المراجِع في تذييل كل تقرير |
| `0015_driver_leaves.sql` | الإجازات + احتساب الراتب بأيام العمل (المقام 30 ثابت) |
| `0016_hire_date_salary.sql` | `hire_date` يدخل حساب أيام العمل + `fn_generate_month_salaries` (زر توليد رواتب الشهر) |
| `0017_profit_uses_earned_salary.sql` | **إصلاح**: دوال الربح كانت تخصم الراتب التعاقدي كاملاً وتتجاهل الإجازة |
| `0018_profit_completeness.sql` | **إصلاح**: عقود الإيجار والسكن تدخل الربح + لوحة التحكم تطابق التقرير المالي |
| `0019_trip_fare_components.sql` | بنود أجرة العميل الأربعة + بدل مبيت السائق + صاحب الطلب + الحالات + سجل من أنشأ/عدّل |
| `0020_monthly_diesel.sql` | الديزل الشهري لكل سائق بدل تسجيله على كل رحلة + حارس الازدواج |
| `0021_company_branches.sql` | سجل فروع العملاء بأكوادها + ربطها بمواقع الرحلة + جلب فروع آخر رحلة |
| `0022_vehicles.sql` | السيارات وأنواعها + ربطها بالسائق والرحلة |
| `0023_waybills.sql` | بوليصة الشحن برقم مميز ثابت |

### الواجهة — البنية العامة
- `src/proxy.ts` + `src/lib/supabase/middleware.ts` — حماية كل الصفحات تلقائياً (أي صفحة غير `/login` محمية بدون استثناء)
- `src/lib/supabase/{client,server}.ts` — عملاء Supabase (متصفح/سيرفر)
- `src/lib/format.ts` — تنسيق الأرقام (en-US لضمان أرقام إنجليزية) والعملة والتاريخ
- `src/lib/arabic.ts` — `normalizeArabic()`: تطبيع النص العربي **للمقارنة فقط** (لا يُحفظ ولا يُعرض). يُستخدم في مطابقة أسماء المدن بخطوط سير السائق
- `src/lib/csv.ts` — تصدير CSV بترميز يدعم العربي
- `src/lib/nav-items.ts` — تعريف القائمة الجانبية بالكامل
- `src/app/globals.css` — إصلاح محاذاة الجداول RTL + توحيد شكل الأرقام (tabular-nums)
- `src/components/layout/{Sidebar,Topbar,DashboardShell}.tsx` — هيكل لوحة التحكم
- `src/components/ui/*` — مكونات عامة (Modal, ConfirmDialog, EmptyState, Pagination, ErrorBanner)

### الصفحات حسب الوحدة
**المصادقة**: `src/app/login/{page,actions}.tsx`

**لوحة التحكم**: `src/app/(dashboard)/dashboard/page.tsx` + `src/components/dashboard/*` (بطاقات، رسوم بيانية، معادلة الربحية)

**السائقون**: `drivers/{page,actions,ledger-actions,custody-actions,route-rates-actions}.ts` + `drivers/[id]/page.tsx` (صفحة تفصيلية فيها: الملخص، السلف، الخصومات، العهدة، ترب خطوط السير) + `components/drivers/*`

**الشركات**: `companies/{page,actions}.ts` + `companies/[id]/page.tsx` + `components/companies/*`

**الرحلات**: `trips/{page,actions}.ts` + `trips/new`, `trips/[id]` (نفس الفورم `TripForm.tsx`) + `trips/import/*` (استيراد Excel)

**الرواتب**: `salaries/{page,actions}.ts` + `components/salaries/*`

**عقود الإيجار الشهري**: `rentals/*` (عقود + وحدات سكن + تقرير ربحية)

**المصروفات**: `expenses/*`

**الإعدادات**: `settings/*`

**الكشوفات** (`statement/`):
| المسار | المحتوى |
|---|---|
| `/statement` | الكشف الداخلي (كل البيانات، سري) |
| `/statement/public` | كشف كامل للسائق (ترب + راتب + خصومات + سلف + عهدة، بدون سعر رحلة/ربح) |
| `/statement/trab` | ترب + سلف + خصومات + عهدة (**بدون راتب**) |
| `/statement/trab-only` | **الترب فقط** — الرحلات ومواقعها والترب، لا شيء آخر |

**التقارير** (`reports/`): `drivers`, `companies`, `profitability` (ربحية الرحلات)، `driver-payments` (تقرير الترب)، `diesel`، `financial` (التقرير المالي الشامل)، `external-drivers` (كشف حساب الموردين الخارجيين)

### التحقق (Validation) — `src/lib/validation/*.ts`
ملف zod schema منفصل لكل كيان: `driver`, `company`, `trip`, `salary`, `ledger` (سلف/خصومات)، `custody`, `routeRate`, `housing`, `rental`, `expense`, `settings`.

### الاستيراد من Excel
- `src/lib/import/parseTripsFile.ts` — يحلل ملف HTML (بامتداد .xls غالباً) لتقرير رحلات، ويكتشف حالة "Frameset" (البيانات في ملف فرعي) ويرشد المستخدم لرفع الملف الصح.
- `src/components/trips/import/ImportWorkflow.tsx` — رفع → معاينة قابلة للتعديل (تحديد صفوف بها تحذير) → تأكيد.
- `src/app/(dashboard)/trips/import/actions.ts` — `parseImportFile` و `confirmImport` (ينشئ شركة/سائقين جدد لو محتاج، بعد موافقة المستخدم في المعاينة).

---

## 3) قاعدة البيانات والجداول

### الجداول الأساسية
- `profiles` — يمتد من `auth.users`، فيه `role` (enum: admin/accountant/employee/driver — الفعّال حالياً admin بس)
- `settings` — صف واحد فقط (اسم المؤسسة، العملة، هل تُحتسب الرحلات الملغاة)
- `drivers` — `salary`, `default_trip_payment` (ترب افتراضي عام)، `extra_stop_rate` (ترب الموقع الإضافي)، `employment_type` (internal/external)
- `companies`
- `trips` — `trip_amount` (سري، محسوب تلقائياً = مجموع كل مواقعه)، `driver_base_payment` (يدوي)، `driver_trip_payment` (محسوب = base + مواقع إضافية × معدل السائق)، `diesel_amount`, `status`
- `trip_locations` — `location_type` (loading/unloading)، `amount`، `sort_order` (ملاحظة: لم يعد يؤثر على الحساب المالي بعد 0011، فقط للترتيب البصري)
- `salaries`, `driver_advances`, `driver_deductions`, `driver_custody_entries` (العهدة، حساب جارٍ متحرك: credit يزيد الرصيد/debit يقلله)
- `expenses`
- `housing_units`, `rental_contracts` (عقود الإيجار الشهري)
- `driver_route_rates` — (`driver_id`, `from_city`, `to_city`, `trab_amount`)

### دوال SQL جاهزة (RPC) — كل الحسابات المالية تتم هنا، مش في الواجهة
- `fn_driver_period_summary`, `fn_driver_internal_trips` — للكشف الداخلي (سري)
- `fn_driver_public_summary`, `fn_driver_public_trips` — للسائق (آمن، بدون سعر/ربح)
- `fn_company_period_summary`, `fn_financial_summary`
- `fn_dashboard_overview`, `fn_monthly_trends`, `fn_top_drivers_profitability`, `fn_top_companies_profitability`
- `fn_drivers_report`, `fn_companies_report`, `fn_driver_payments_report`, `fn_trips_report_totals`
- `fn_external_drivers_report`, `fn_driver_custody_balance`
- `fn_rental_contracts_report`
- `recalc_trip_totals` / trigger `sync_trip_amount` — **الأهم**: يعيد حساب `trip_amount` و`driver_trip_payment` تلقائياً عند أي تغيير في مواقع الرحلة

### الأمان
RLS مفعّل على **كل** جدول. سياسة موحّدة: `is_admin()` (دالة تتحقق من `profiles.role`). لا يوجد وصول عام (anon) لأي بيانات.

---

## 4) آخر التعديلات (بالترتيب الزمني، الأحدث أولاً)

0. **مصروفات السائق + تصفية التربات (0012 و0013)** — أكبر إضافة منذ 0008. التفاصيل في §13 أسفل.
1. **تطبيع مطابقة أسماء المدن** (`src/lib/arabic.ts`): مطابقة خطوط سير السائق كانت `trim().toLowerCase()` فقط — و`toLowerCase()` لا يؤثر على الحروف العربية إطلاقاً، فكانت المطابقة حرفية تماماً. دلوقتي بتتجاهل الفروق الإملائية الشائعة، وبيظهر تنبيه أصفر لما مايتطابقش خط سير رغم وجود خطوط محفوظة للسائق. ✅ `npm run build` نجح و TypeScript مرّ بلا أخطاء، و10 حالات اختبار سلوكي نجحت (منها حالتان سلبيتان تحرسان من الإفراط في التطبيع).
2. **تقريرين جداد**: كشف الترب (بدون راتب) و كشف الترب فقط — أزرار جديدة في `/statement`.
3. **تصحيح جوهري (0011)**: فصل سعر الموقع الإضافي للعميل عن ترب السائق (كان في خطأ في 0009 خلط بينهم).
4. **ترب حسب خط السير (0010)**: كل سائق يقدر ياخد ترب مختلف حسب (مدينة تحميل → مدينة تنزيل)، بيتملى تلقائياً في نموذج الرحلة.
5. **تسمية "ترب"**: استُبدلت كل كتابات "تربة/تربه/تربات" بـ "ترب" في كل الواجهة (طلب المستخدم صراحة).
6. **(0009)**: حذف مفهوم "مؤقت/مثبت" لمبالغ المواقع بالكامل، وربط أول موقع تنزيل فقط بالسعر (لاحقاً تم تصحيح هذا في 0011).
7. **توحيد الخط والأرقام**: كل الأرقام بترقيم إنجليزي (كانت تظهر بأرقام هندية بسبب locale=ar-SA)، وحذف استخدام font-mono غير المتسق.
8. **إصلاح محاذاة الجداول RTL**: قاعدة CSS عامة (`[dir="ltr"] { text-align: right }`).
9. **العهدة + الموردون الخارجيون + عقود الإيجار (0008)**: أكبر إضافة دفعة واحدة.
10. **استيراد Excel**: يدعم ملفات HTML (تصدير Excel الشائع من الأنظمة الأخرى) مع معاينة قابلة للتعديل.

---

## 5) المشاكل الحالية / القيود المعروفة

- **`src/types/database.ts` لسه placeholder** (`export type Database = any`) — الأنواع الحقيقية لسه ما اتولّدتش من Supabase (`supabase gen types`). ده معناه TypeScript مش بيتحقق بدقة من استعلامات Supabase — شغّال، بس فيه مخاطرة أخطاء وقت التشغيل مش وقت البناء.
- **`sort_order`** في `trip_locations` بقى شبه بلا فائدة فعلياً بعد إصلاح 0011 (الحساب المالي بقى بالعدد مش بالترتيب) — لسه موجود بالعمود بس مش مؤثر.
- **مطابقة خطوط السير** في نموذج الرحلة بقت تتجاهل الفروق الإملائية العربية الشائعة (`src/lib/arabic.ts`): أشكال الألف (أ/إ/آ/ٱ)، التاء المربوطة والهاء (ة/ه)، الألف المقصورة والياء (ى/ي)، الهمزات (ؤ/ئ)، **الحروف الفارسية المتسللة (ی/ک) اللي بتبان مطابقة للعين وهي نقاط ترميز مختلفة**، التشكيل، التطويل، الأرقام العربية-الهندية والفارسية، والمسافات المتكررة. **لكنها لسه حرفية فيما عدا ذلك** — اختلاف حقيقي في الاسم (اختصار، اسم بديل للمدينة، خطأ إملائي في حرف أصلي مثل "الريّاض"/"الرباض") مش هيتطابق. عمداً **لا** تُحذف أداة التعريف "ال" لأن "العلا" و"علا" مدينتان مختلفتان. لما مايتطابقش أي خط سير رغم وجود خطوط محفوظة للسائق، النموذج بيعرض تنبيه أصفر بدل ما يفضل صامت.
- **استيراد Excel**: المحلل (`parseTripsFile.ts`) مبني على تنسيق ملفين محددين شافهم Claude فعلياً (فيهم أعمدة زي "الأجرة الأساسية"، "تكلفة المورد"...). لو العميل غيّر تنسيق التصدير، المحلل ممكن يفشل أو يطلع صفوف "تحتاج مراجعة".
- **لا يوجد اختبار آلي (tests)** — كل التحقق تم عبر `tsc --noEmit` + `npm run build` + اختبار المستخدم يدوياً على الموقع الفعلي.
- **Git identity**: كان فيه خلط بين حسابين GitHub (`mwsfaty-afk` الشخصي و`omar20302024-commits` بتاع المشروع) — تم ضبط `git config` محلياً (مش عالمياً) على مستودع المشروع بس ليستخدم `omar20302024@gmail.com`. الـ commits القديمة (قبل الإصلاح) لسه باسم الحساب الشخصي في السجل التاريخي — لم تُعدَّل (يحتاج rebase لو حابب تصحيحها، لم يُطلب).

---

## 6) الأشياء التي تم إنجازها

كل الـ 23 Phase الأصلية (البنية، قاعدة البيانات، المصادقة، لوحة التحكم، CRUD كامل للسائقين/الشركات/الرحلات/الرواتب/السلف والخصومات، الكشوفات الداخلية والعامة، كل التقارير الستة، الطباعة، تصدير CSV، مراجعة أمان RLS، مراجعة أداء) **بالإضافة إلى**:

- عقود الإيجار الشهري + وحدات السكن (مع تقسيم تكلفة السكن تلقائياً على عدة سائقين)
- تصنيف السائقين (داخلي/مورد خارجي) + كشف حساب مخصص للموردين
- العهدة (حساب جارٍ متحرك لكل سائق)
- ترب حسب خط السير لكل سائق + ترب افتراضي عام + ترب موقع إضافي (معدل ثابت لكل سائق)
- استيراد رحلات من ملف Excel/HTML مع مطابقة/إنشاء سائقين وشركات
- تقريرين إضافيين للسائق: كشف الترب (بدون راتب)، كشف الترب فقط
- إصلاحات: محاذاة RTL، الأرقام الإنجليزية، توحيد الخط، تسمية "ترب"، فصل سعر العميل عن ترب السائق للمواقع الإضافية

النظام **منشور فعلياً وشغّال** على Vercel، متصل بمشروع Supabase حقيقي، والكود على GitHub.

---

## 7) الأشياء المتبقية / اقتراحات مستقبلية (لم تُطلب بعد كمهام محددة)

- توليد أنواع TypeScript حقيقية من Supabase بدل الـ placeholder (`supabase gen types typescript`).
- اختبار شامل لكل سيناريوهات استيراد Excel من ملفات حقيقية متنوعة أكتر.
- لو حبيت تفعيل دور "سائق" (يدخل بنفسه يشوف كشفه) — البنية جاهزة لذلك (`user_role` enum فيه القيمة) لكن لم تُبنَ شاشات/سياسات RLS لذلك بعد.
- لا يوجد حالياً واجهة لإدارة المستخدمين (إضافة محاسب/موظف) — كل شيء admin واحد حالياً.
- تنظيف تاريخ Git من commits باسم الحساب الشخصي (اختياري، مذكور في المشاكل أعلاه).

---

## 8) تعليمات التشغيل

### التطوير محلياً
```bash
cd E:\claude\logistic
npm install
npm run dev
```
افتح `http://localhost:3000` — هيوجّهك لصفحة تسجيل الدخول تلقائياً.

### متغيرات البيئة (`.env.local`، غير موجود في Git)
```
NEXT_PUBLIC_SUPABASE_URL=https://yrzciifapkcbxyrwnebm.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon key من Supabase Dashboard>
```
⚠️ لا تضع `service_role key` في أي مكان بالكود أو في هذا الملف.

### تشغيل ملفات SQL على مشروع Supabase
اذهب لـ **Supabase Dashboard → SQL Editor**، وشغّل ملفات `supabase/migrations/*.sql` **بالترتيب الرقمي من 0001 إلى 0011**، كل ملف لوحده (انسخ المحتوى كاملاً، مش اسم الملف).

### البناء والنشر
```bash
npm run build   # تحقق قبل أي commit
```
النشر تلقائي على Vercel عند أي push لفرع `master` (الرابط الحالي: `logistic-sa.vercel.app`).

### حساب Admin أول
Supabase Dashboard → Authentication → Users → Add User (فعّل Auto Confirm) — أول مستخدم يتسجل بيبقى admin تلقائياً (trigger `handle_new_user`).

### حساب GitHub الصحيح
المستودع: `github.com/omar20302024-commits/logistic.git` — استخدم حساب `omar20302024-commits` (البريد: omar20302024@gmail.com) عند الـ push، مش أي حساب شخصي تاني.

---

## 9) القرارات التي اتخذناها أثناء بناء النظام

- **Next.js 16 (App Router) + TypeScript + Tailwind v4 + Supabase (Postgres + Auth + RLS)** — بدون أي باك-إند منفصل.
- **كل حساب مالي يتم داخل قاعدة البيانات** (generated columns / triggers / SQL functions)، مش في JavaScript — لضمان تطابق الأرقام في كل الشاشات ودقة الفاصلة العشرية (`numeric(12,2)` في كل مكان).
- **فصل الكشف الداخلي عن كشف السائق على مستوى الكود** (مكوّنات ودوال SQL منفصلة تماماً)، مش مجرد إخفاء عمود في الواجهة.
- **middleware أصبح اسمه `proxy.ts`** في Next.js 16 (تغيير رسمي في الإطار، اكتُشف أثناء البناء) — لازم يكون داخل `src/` مع مشروع بـ `src-dir`.
- **حذف الحذف الفعلي (hard delete) ممنوع** للسائقين/الشركات إذا عندهم رحلات (`ON DELETE RESTRICT`) — الحل تغيير الحالة لـ"غير نشط".
- **الطباعة عبر CSS مخصص (`@media print`)** بدل مكتبة PDF منفصلة — أبسط وأسرع للنشر على Vercel.
- **العهدة كحساب جارٍ (رصيد متحرك بعمليتين: credit/debit)** بدل حقل ثابت — لأن الشركة ممكن تدي السائق فلوس أو يحصّل من عميل (كلاهما زيادة)، والسائق ممكن يصرف من جيبه لو مفيش عهدة (فيتحول الرصيد لسالب = مستحق له).
- **تقسيم تكلفة السكن تلقائياً** بين كل عقود الإيجار المرتبطة بنفس وحدة السكن في نفس الشهر (بدل إدخال يدوي لكل سائق).
- **سعر الموقع الإضافي (للعميل) منفصل تماماً عن ترب الموقع الإضافي (للسائق)** — الأول مبلغ حر لكل رحلة، الثاني معدل ثابت محفوظ لكل سائق.
- **استيراد Excel بمراجعة إلزامية قبل الحفظ** — لا يُنشأ أي سائق/شركة جديد بدون عرضه على المستخدم أولاً في شاشة معاينة قابلة للتعديل.

---

## 10) قواعد وشروط طلبها المستخدم صراحةً (لازم تتراعى في أي تعديل مستقبلي)

1. **لا تستخدم Excel/Google Sheets/LocalStorage كقاعدة بيانات** — Supabase فقط.
2. **RTL كامل**، خط عربي حديث (Cairo)، تصميم احترافي لا Prototype شكلي.
3. **سعر الرحلة سرّي تماماً** — لا يظهر أبداً في أي كشف يخص السائق (حتى بالطباعة/PDF).
4. **الترب مرتبط بالرحلة، الراتب مصروف شهري مستقل** — لا يُخصم الراتب من ربح رحلة بعينها أبداً.
5. **كل الحسابات تلقائية بالكامل** — لا يُدخل أي إجمالي أو ربح يدوياً في أي مكان.
6. **حذف رحلة يحذف مواقعها** ويحدّث كل التقارير فوراً (لا أرقام مخزَّنة يدوياً).
7. **الرحلات الملغاة لا تدخل في حسابات الربح افتراضياً** (إعداد قابل للتغيير).
8. **الاسم الصحيح "ترب"** — ممنوع "تربة" أو "تربه" أو حتى "تربات" في أي واجهة جديدة.
9. **مفيش "مؤقت/مثبت"** لمبالغ المواقع — أُزيل نهائياً، لا يُعاد.
10. **سعر الموقع الإضافي للعميل ≠ ترب الموقع الإضافي للسائق** — دايماً منفصلين، كل سائق له معدله وكل عميل سعره.
11. **كشف السائق العام يجب ألا يحتوي أي بيانات سرية أبداً** (سعر رحلة/ربح/ديزل) — حتى لو طُلب تعديل مستقبلي، هذا القيد لا يُكسر.
12. **قبل أي تنفيذ لميزة كبيرة، يُعرض التصميم ويُسأل عن القرارات المفتوحة أولاً** (نمط عمل متكرر طلبه المستخدم أكثر من مرة: "أعطني إجابات قبل التنفيذ").
13. **الأمان**: لا تُدخل أي كلمة مرور أو مفتاح `service_role` في الكود أو المحادثة — الـ `anon key` فقط آمن للمشاركة.
14. **لا عمود "مواقع التنزيل" في أي كشف يخص السائق** — أُزيل من `/statement/public` و`/statement/trab` و`/statement/trab-only` بطلب صريح. يبقى في الكشف الداخلي `/statement` فقط (للإدارة).
15. **تذييل المراجعة إلزامي في كل تقرير**: «تمت المراجعة بواسطة ...» من `settings.reviewed_by`. أي تقرير أو كشف جديد لازم يضيف `<ReviewedByFooter />`.
16. **الراتب يُحسب بأيام العمل**: المقام 30 يوماً دائماً، ولا يوم راحة أسبوعي (السائقون يعملون كل أيام الشهر)، ولا وجود لإجازة مدفوعة.

---

## 11) آخر نقطة وصلنا إليها

آخر تعديل مُنفَّذ ومنشور (commit `ab344a2`، مدفوع لـ `origin/master`، ومنشور تلقائياً على Vercel):
**إضافة تقريرين جديدين لكشف السائق**:
- `/statement/trab` — الترب + السلف + الخصومات + العهدة (بدون راتب)
- `/statement/trab-only` — الترب فقط (رحلات ومواقع وترب، لا شيء آخر)

كل ملفات SQL حتى `0011` تم تشغيلها وتأكيدها شغّالة على مشروع Supabase الحقيقي (`yrzciifapkcbxyrwnebm`). النظام في حالة مستقرة وقابلة للاستخدام الفعلي الآن.

### ⚠️ تعديل غير محفوظ في Git وغير مُتحقَّق منه (جلسة 2026-09-10)

بعد الـ commit ده تم تعديل مطابقة أسماء المدن، وهو **لسه في شجرة العمل فقط** (لا commit ولا push ولا نشر):

| الملف | التغيير |
|---|---|
| `src/lib/arabic.ts` | **ملف جديد** — `normalizeArabic()` للمقارنة فقط |
| `src/components/trips/TripForm.tsx` | `norm` بقى `normalizeArabic` + تنبيه أصفر عند عدم تطابق خط السير |
| `PROJECT_CONTEXT.md` | تحديث المسار (§8) و§2 و§4 و§5 |

**تم التحقق منه:**
- ✅ `npm run build` نجح (Next 16.3.4 / Turbopack)، و TypeScript مرّ بلا أخطاء، وكل الـ 28 مساراً اتبنت.
- ✅ 10 حالات اختبار سلوكي لـ `normalizeArabic` نجحت كلها، أهمها `"٢٠٢٤"` → `"2024"` (الحارس ضد خطأ نطاق التشكيل الموصوف في رأس `arabic.ts`)، وحالتان سلبيتان: "جدة" ≠ "الدمام" و"العلا" ≠ "علا".

✅ **اتعمل commit ودفع**: `c629ca2` على `origin/master`، بحساب `omar20302024-commits` الصحيح (المؤلِّف والمُودِع كلاهما)، ونشر تلقائياً على Vercel.

**اللي لسه ناقص — اختبار يدوي في المتصفح (لم يُنفَّذ بعد):**
1. سائق له خط سير محفوظ **"جدة → الرياض"**. افتح رحلة جديدة، اختر السائق، واكتب في "من/إلى": **"جده"** و **"الریاض"** (لاحظ: تاء مربوطة ناقصة + ياء فارسية ی) — لازم الترب يتملى من خط السير ويظهر "✓ مطابق لخط سير محفوظ". قبل التعديل ده كان هيرجع للترب الافتراضي بصمت.
2. اختبار عكسي: اكتب مدينة مختلفة فعلاً (مثلاً "الدمام") — لازم يظهر التنبيه الأصفر، مش الأخضر.

---

## 12) بيئة الجهاز الحالي (E:\claude\logistic)

- **Node**: v24.19.0 (المطلوب ≥ 20.9)، **npm**: 11.17.0، **Git**: 2.55.0 — كلهم مثبتين عبر `winget`.
- **`node_modules` مثبَّت بالفعل** ومعاه الملفات الثنائية الصحيحة للمنصة (`@next/swc-win32-x64-msvc`, `@tailwindcss/oxide-win32-x64-msvc`) — مفيش داعي لـ `npm install`.
- **`.env.local` موجود** وفيه المفتاحين.
- **هوية Git والـ remote محفوظين محلياً** في `.git/config` على `omar20302024-commits` — مش محتاج تضبطهم من جديد.
- **بيانات اعتماد GitHub** محفوظة في Windows Credential Manager بعد أول `push` تفاعلي.

### فخّان متكرران على ويندوز (اتقابلا فعلاً)
1. **PATH**: أي نافذة طرفية (أو جلسة Claude) مفتوحة **قبل** تثبيت Node/Git مش هتشوفهم — لازم تُغلق وتُفتح من جديد. لو لزم الأمر جوّه أمر واحد: `$env:PATH = "C:\Program Files\Git\cmd;C:\Program Files\nodejs;" + $env:PATH`.
2. **سياسة تنفيذ PowerShell**: `npm` سكربت `.ps1`، والسياسة الافتراضية بتمنعه. الحل: `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned` (مش محتاج صلاحيات مدير)، أو استخدام `npm.cmd`.

---

## 13) مصروفات السائق + تصفية التربات (0012 و0013)

### ⚠️ ترتيب النشر إلزامي
**شغّل `0012` ثم `0013` على Supabase قبل نشر الكود.** الواجهة الجديدة بتكتب أعمدة
(`reason`, `expense_category`, `settlement_id`) وبتنادي دوال (`fn_create_driver_settlement`,
`fn_driver_settlement_preview`) لسه مش موجودة. لو اتنشر الكود الأول، إضافة حركة عهدة هتفشل.

### أ) مصروفات دفعها السائق

كانت العهدة بتوصّل الفلوس للسائق صح، بس كان فيه ثغرتين:

1. **`debit` كان بيخلط حالتين محاسبيتين مختلفتين**: "صرف على مصروف عمل" (مصروف حقيقي)
   و"أرجع مبلغاً للشركة" (حركة نقدية). من غير تفرقة، أي دمج بالربح كان هيحتسب
   المُرجَع كمصروف.
2. **مصروف السائق ما كانش بيدخل الربح إطلاقاً** — `net_profit` معادلتها ما فيهاش العهدة.
   فلو دفع 500 ديزل من جيبه، كان بياخدهم (صح) والربح يفضل مبالغ فيه بـ 500 (غلط).

الحل: `reason` بخمس قيم، و`work_expense` **وحده** هو اللي بيتخصم من الربح عبر
`fn_driver_paid_expenses`. القيود القديمة كلها `unspecified` **ولا تمسّ الربح** — عشان
الأرقام التاريخية ما تتغيّرش فجأة؛ صنّفها يدوياً على مهلك.

🛡️ **حارس ازدواج الديزل**: لو ربطت مصروف بند "ديزل" برحلة مسجَّل عليها `diesel_amount`،
النموذج بيحذّرك — دي الحالة الوحيدة اللي الديزل ممكن يتحسب فيها مرتين.

### ب) تصفية التربات

`driver_settlements` بأرقام **مجمّدة** وقت الصرف (مش محسوبة من جديد) — عشان لو رحلة
اتعدّلت بعد الصرف، السند يفضل شاهداً على المبلغ اللي اتصرف فعلاً.

**منع الازدواج على مستوى الرحلة**: كل رحلة بتتوسم بـ `settlement_id`. يعني رحلة بتتسجل
متأخرة بتاريخ قديم جوّه فترة متصفّية هتفضل بدون تصفية وتظهر في السند اللي بعده، بدل ما
تضيع أو تتحسب مرتين. نفس الوسم على حركات العهدة والسلف والخصومات.

**السلف والخصومات — مرونة بسلامة محاسبية**: عمود `settle_against` (`salary` أو `trabs`).
كل سلفة تنتمي لدلو **واحد بس**، والقيد `chk_*_bucket_matches_settlement` بيمنع أي عنصر
محمّل على الراتب إنه يتوسم بسند. فالخصم المزدوج مستحيل بنيوياً، مش مجرد اتفاق.
الافتراضي `salary` فكل البيانات القديمة زي ما هي بالظبط. وقت التصفية بتختار بالـ checkbox
أنهي سلفة تتحمّل على التربات في السند ده، والباقي يفضل مع الراتب.

**الراتب مستقل تماماً** عن التصفية (قاعدة #4). التقرير الشامل بالراتب موجود في
`/statement/public` زي ما هو.

### ج) تصحيحان اتكشفا أثناء التنفيذ

1. `fn_driver_custody_balance` كانت بتجمع **كل** القيود بما فيها المتصفّية. فبعد ما
   تصرف للسائق رصيد سالب، الرصيد كان هيفضل سالب والكشف يطالب بنفس المبلغ تاني.
   دلوقتي بتحسب `settlement_id is null` بس.
2. `total_due_to_driver` كانت بتجمع كل تربات الفترة حتى المصروف منها. بقت تستخدم
   `unsettled_trabs` والسلف المفتوحة بس.

### د) ملفات الوحدة

| الملف | الدور |
|---|---|
| `src/lib/validation/custody.ts` | أسباب العهدة + بنود المصروف + تحقق تقاطعي |
| `src/lib/validation/settlement.ts` | مخطط التصفية والأنواع المشتركة |
| `src/app/(dashboard)/drivers/settlement-actions.ts` | المرشحون + المعاينة + الإنشاء + الإلغاء |
| `src/components/drivers/SettlementsSection.tsx` | قائمة التصفيات في صفحة السائق |
| `src/components/drivers/SettlementFormModal.tsx` | معالج التصفية بمعاينة حيّة |
| `src/components/settlements/SettlementVoucher.tsx` | سند الطباعة |
| `src/app/(dashboard)/settlements/[id]/page.tsx` | صفحة السند |

🔒 **السند لا يستقبل ولا يعرض** `trip_amount` ولا `trip_profit` ولا `diesel_amount` —
الأعمدة السرية دي مش بتتطلب من قاعدة البيانات من الأساس (قاعدتا #3 و#11).

### هـ) ما لم يُختبر بعد
`npm run build` نجح و TypeScript مرّ بلا أخطاء. لكن **ملفات SQL لم تُشغَّل** (مفيش
Postgres محلي) ولا يوجد اختبار متصفح. المطلوب: شغّل 0012 و0013، ثم جرّب دورة كاملة —
قيد مصروف عمل، تصفية فترة، مراجعة السند، ثم إلغاء التصفية والتأكد إن كل حاجة رجعت.

---

## 14) الإجازات واحتساب الراتب بأيام العمل (0015) + تذييل المراجعة (0014)

### أ) قواعد الراتب المتفق عليها صراحةً

السائقون مغتربون، **يعملون كل أيام الشهر بلا أي يوم راحة أسبوعي** (لا جمعة ولا غيرها)،
وإجازتهم قد تمتد شهراً أو شهرين. وعليه:

| القاعدة | القيمة |
|---|---|
| المقام | **30 يوماً دائماً** — لا أيام الشهر الفعلية |
| أيام الراحة | **لا وجود لها** — كل أيام التقويم في الإجازة تُخصم |
| إجازة مدفوعة | **غير مطلوبة** — كل إجازة تُخصم بعدد أيامها |

```
أيام العمل     = greatest(0, 30 − أيام الإجازة داخل الشهر)
الراتب المستحق = الراتب الأساسي × أيام العمل ÷ 30
```

في شهر 31 يوماً، من يعمله كله يأخذ راتبه كاملاً بلا زيادة. ومن كان في إجازة الشهر كله
يصل لصفر (الـ `greatest(0, …)` يمنع السالب).

**مثال**: راتب 3000، إجازة من 11 سبتمبر لآخر الشهر → أيام عمل 10 → المستحق **1000**.

الراتب الأساسي يبقى كامل الشهر في `salaries`؛ المستحق **يُحسب** ولا يُدخل يدوياً (قاعدة #5).

### ب) لماذا `fn_driver_earned_salary` دالة مستقلة

لأن الرقم يُقرأ من مكانين: `v_salary_statements` (شاشة الرواتب) و`fn_driver_period_summary`
(كشف السائق الذي تُرسله له). لو حُسب في كل مكان على حدة، كان أول تعديل مستقبلي على
المعادلة سيجعل الشاشتين تعرضان رقمين مختلفين لنفس الشهر.

⚠️ **أهم سطر في 0015**: `sal_agg` في `fn_driver_period_summary` صار يجمع **المستحق** لا
الأساسي. بدونه كان يُرسل للسائق راتب شهر كامل بينما تصفيته على 10 أيام.

### ج) الإجازة المفتوحة

`to_date` قابل للفراغ = لم يُحدَّد تاريخ الرجوع بعد. **خطرة**: تُصفّر رواتب كل الأشهر
التالية بصمت لو نُسي إغلاقها. لذلك يظهر وسم أصفر بارز في صفحة السائق، وتحذير في نموذج
الإجازة نفسه.

🛡️ قيد `no_overlapping_driver_leaves` (‏`exclude using gist`) يمنع تداخل إجازتين لنفس
السائق على مستوى قاعدة البيانات — بدونه تسجيل نفس الإجازة مرتين بالخطأ يخصم أيامها
مرتين ويقلّل الراتب دون أن يلاحظ أحد. يحتاج امتداد `btree_gist` (متاح في Supabase
ويُفعَّل تلقائياً في الملف).

### د) تذييل المراجعة (0014)

`settings.reviewed_by` افتراضياً «م/محمود جعفر»، ويظهر أسفل **13 تقريراً وكشفاً** عبر
`<ReviewedByFooter />`. الاسم إعداد في قاعدة البيانات لا قيمة ثابتة في الكود: تغييره من
شاشة الإعدادات لا من نشر جديد، واسم شخص متكرر في 13 ملفاً يصير مستحيل تغييره بأمان.
تركه فارغاً يخفي التذييل.

### هـ) ملفات الوحدة

`src/lib/validation/leave.ts` · `drivers/leave-actions.ts` ·
`components/drivers/{LeavesSection,LeaveFormModal}.tsx` · `components/ui/ReviewedByFooter.tsx`

---

## 15) تاريخ التعيين وتوليد الرواتب (0016)

### القاعدة الحاسمة: الراتب لا علاقة له بالرحلات إطلاقاً

سائق معيَّن وليس له ولا رحلة واحدة **يأخذ راتبه كاملاً**. الرحلات تخصّ الترب، لا الراتب
(قاعدة #4). `fn_driver_earned_salary` لا تمسّ جدول الرحلات في أي سطر.

### أ) `hire_date` صار يدخل الحساب

كان موجوداً من `0001` ويُدخَل في نموذج السائق ويُعرض في الجدول، لكنه **لم يكن يدخل أي
حساب**. الآن:

```
أيام قبل التعيين = أيام الشهر الواقعة قبل تاريخ التعيين
أيام العمل       = greatest(0, 30 − أيام قبل التعيين − أيام الإجازة)
```

من يُعيَّن في 15 الشهر يأخذ 16 يوماً. ومن يُعيَّن بعد نهاية الشهر يأخذ صفراً.

⚠️ **أيام الإجازة تُقصّ عند تاريخ التعيين** داخل `fn_driver_leave_days`. بدون هذا القص،
إجازة مسجَّلة بتاريخ سابق للتعيين كانت تُخصم **مرتين**: مرة كأيام قبل تعيين ومرة كإجازة.

`0016` يضبط تاريخ تعيين **كل** السائقين على `2026-09-01` (طلب صريح)، ويكتب فوق أي تاريخ
سابق. أي سائق له تاريخ مختلف يُعدَّل من نموذجه بعد التشغيل. والافتراضي للسائقين الجدد صار
`current_date`.

### ب) زر «توليد رواتب الشهر»

الراتب كان لا يظهر إلا بوجود صف في `salaries` تُنشئه يدوياً لكل سائق كل شهر — فسائق بلا
سجل كان كشفه يقول «الراتب = صفر»، وهذا يناقض «راتبه من تاريخ التعيين».

`fn_generate_month_salaries(year, month)` تُنشئ سجلاً لكل سائق **داخلي نشط** معيَّن قبل
نهاية الشهر، بقيمة `drivers.salary`. الموردون الخارجيون مستثنون — هؤلاء يأخذون ترباً لكل
رحلة لا راتباً شهرياً.

🛡️ **آمن للتكرار**: `not exists` + قيد `unique(driver_id, month, year)`. الضغط مرتين لا
يضاعف شيئاً — الموجود يُترك والناقص يُضاف.

الزر في شاشة الرواتب يستخدم الشهر المحدَّد في الفلتر، وإلا الشهر الحالي، ويؤكّد قبل
التنفيذ.

### ج) لماذا بقي جدول `salaries` موجوداً

لأنه يحمل ما لا يمكن اشتقاقه: `paid_amount` و`payment_date` (المدفوعات الفعلية)، و
`basic_salary` كقيمة قابلة للتعديل الاستثنائي في شهر بعينه دون المساس بالعقد.

---

## 16) إصلاح أساس الراتب في دوال الربح (0017)

### العيب

`0015` و`0016` جعلا الراتب يُحسب بأيام العمل، لكن التمرير لم يصل لدوال الربح. فصارت
كل شاشة تقول رقماً مختلفاً:

| الدالة | كانت تستخدم |
|---|---|
| `fn_driver_period_summary` · `v_salary_statements` | المستحق ✅ |
| `fn_financial_summary` · `fn_dashboard_overview` · `fn_drivers_report` | التعاقدي ❌ |

سائق راتبه 3000 وفي إجازة 20 يوماً: تكلفته الحقيقية 1000، والربح كان يخصم 3000 —
**فالربح يظهر أقل مما هو**، والفارق يكبر كلما زادت الإجازات.

### الإصلاح

الدوال الثلاث تستدعي الآن `fn_driver_earned_salary` — **نفس الدالة** التي يستدعيها كشف
السائق وشاشة الرواتب. فمستحيل أن تفترق الأرقام مرة أخرى.

`salaries.basic_salary` يبقى كما هو (الراتب التعاقدي كامل الشهر). الخصم بأيام العمل
يحدث **وقت القراءة**، لا بتعديل البيانات المخزَّنة.

⚠️ `fn_drivers_report` أُعيد تسمية عموده `salary_basic` → `salary_earned`. الاسم القديم
صار كذباً على القارئ بعد أن صار المحتوى المستحق لا التعاقدي. وعُدِّلت التسمية في الواجهة
إلى «الراتب المستحق»، وفي التقرير المالي إلى «الرواتب (حسب أيام العمل)».

### العيبان الآخران أُصلحا في 0018

كانا مذكورين هنا كمتروكين، ثم أُصلحا — التفاصيل في §17.

---

## 17) اكتمال معادلة الربح (0018)

### أ) عقود الإيجار الشهري دخلت الربح أخيراً

كانت خارجه تماماً **منذ `0008`**: `monthly_amount` (إيراد) و`diesel_amount` و
`housing_units.monthly_rent` (تكاليف) تظهر في `fn_rental_contracts_report` وحدها.
خط نشاط كامل بإيراداته وتكاليفه خارج صافي الربح.

🔑 **لماذا الإضافة آمنة**: سائقو عقود الإيجار **لا تُسجَّل لهم رحلات** في جدول `trips`
(مؤكَّد من المستخدم). فإضافة `monthly_amount` كإيراد لا تكرّر أي إيراد رحلات. ورواتبهم
محسوبة أصلاً ضمن `sal_agg` الذي يجمع كل صفوف `salaries`، فلا تُضاف ثانيةً.

لو تغيّرت طريقة الإدخال يوماً وصارت رحلات هؤلاء تُسجَّل بأسعارها، **سيتضاعف الإيراد** —
هذه هي النقطة الوحيدة التي تنهار عندها المعادلة.

**تكلفة السكن**: `fn_rental_period_totals` تحسب الوحدة مرة واحدة عن كل شهر فيه عقد واحد
على الأقل يستخدمها — مطابق تماماً لمنطق التقسيم في `fn_rental_contracts_report`، حتى لا
يوجد رقمان مختلفان لنفس التكلفة.

⚠️ وحدة سكن بلا أي عقد في شهر معيّن **لا تُحتسب**. إن كنت تدفع إيجارها وهي فارغة، سجّله
في `expenses` — هذه تدخل الربح.

### ب) لوحة التحكم والتقرير المالي صارا متطابقين

`0012` أضاف مصروفات السائقين إلى `fn_financial_summary` وحدها، فكانت الشاشتان تعطيان
رقمين مختلفين. الآن كلتاهما تستدعيان `fn_rental_period_totals` و`fn_driver_earned_salary`
وتخصمان نفس البنود بالضبط.

### المعادلة النهائية

```
الربح التشغيلي = (قيمة الرحلات − الترب − ديزل الرحلات)
                + (إيراد عقود الإيجار − ديزل العقود)

صافي الربح     = الربح التشغيلي
                − الرواتب (المستحقة بأيام العمل)
                − مصروفات دفعها السائقون
                − إيجار السكن
                − المصروفات الأخرى
```

### ج) نمط تكرّر ثلاث مرات — يستحق الانتباه

نفس العيب تكرّر في `0012` و`0017` و`0018`: بند يُضاف أو يتغيّر في دالة ربح واحدة ويُنسى
في الأخريات، فتفترق الشاشات. **عند أي تعديل مستقبلي على معادلة الربح، عدّل
`fn_financial_summary` و`fn_dashboard_overview` معاً دائماً**، وتحقّق من
`fn_drivers_report` كذلك.

---

## 18) مواءمة نظام الرحلات مع wattam (0019 — 0023)

مجلد `wattam/` نسخة مرجعية من نظام PHP آخر للمستخدم (**مُستثنى من Git** — فيه `config/`
ونسخة احتياطية قد تحوي بيانات اتصال أو بيانات عملاء). طُلب استيراد مزاياه الناقصة
**مع إبقاء الترب والتصفيات كما هي** — إضافة بلا هدم.

### التعارض الجوهري الذي حكم كل القرارات

**wattam لا يعرف «الترب» إطلاقاً.** جدول سائقيه فيه `salary` فقط، ومعادلة ربحه:

```
ربح wattam   = إجمالي الأجرة − تكلفة المورد     (السائق الداخلي بلا تكلفة على الرحلة)
ربح logistic = سعر الرحلة − الترب − الديزل
```

فالنقل الحرفي كان سيهدم `0009`–`0013` كلها. لذلك أُخذ **جانب العميل فقط** من wattam،
وتُرك جانب السائق كما هو.

### أ) بنود الأجرة (0019)

```
سعر الرحلة = الأساسية + العمالة + الموقع الإضافي + المبيت
ترب السائق  = الترب الأساسي + (المواقع الإضافية × معدَّل السائق) + بدل المبيت
```

المرتجع من wattam حُذف بطلب المستخدم (يُسجَّل كرحلة عادية). والمبيت أُضيف لأن السائق قد
ينتظر لليوم التالي.

🔑 **الجانبان صارا متناظرين أخيراً**: `companies.extra_location_rate` للعميل مقابل
`drivers.extra_stop_rate` للسائق — لكلٍّ معدَّله، ولا أحدهما مشتقّ من الآخر. هذا ما
قصدته القاعدة #10 دائماً.

⚠️ **بدل مبيت السائق حقل لكل رحلة لا معدَّل ثابت**، لأن المستخدم قال إنه يستحقه «في بعض
الأوقات» — ومعدَّل دائم كان سيصرفه حتى حين لا يستحق.

**الترحيل**: كل رحلة قديمة أخذت `base_fare` = سعرها الحالي، فلم يتغيّر رقم واحد.
`trip_locations.amount` تحوّل من مصدر السعر إلى توزيع داخلي على المواقع.

### ب) الديزل الشهري (0020)

انتقل من كل رحلة إلى سجل واحد لكل سائق في الشهر. `trips.diesel_amount` وقيمه التاريخية
لم تُمسّ؛ حُذف الحقل من **نموذج الإدخال** فقط.

⚠️ **ربحية الرحلة الواحدة لم تعد متجانسة عبر نقطة التحويل**: رحلة قديمة ربحها بعد خصم
ديزلها، وجديدة قبله. صافي الربح سليم في الحالتين (الديزل الشهري يُخصم تحت الربح
التشغيلي كالرواتب)، لكن لا تُقارن ربحية رحلتين من جانبَي التحويل.

🛡️ `fn_diesel_overlap_check` ترصد سائقاً له ديزل على رحلاته **وكذلك** سجل شهري في نفس
الشهر — الحالة الوحيدة للازدواج، وتقع غالباً في شهر التحويل.

### ج) الفروع (0021) والسيارات (0022)

سجل فروع لكل عميل بكود مميز داخل العميل الواحد، وسجل سيارات وأنواعها (الأنواع في جدول
لا `enum` فتُضاف من الشاشة).

**كلاهما اختياري بالكامل** ولا يمسّ أي رحلة قديمة. والاقتراحات (سيارة السائق، فروع آخر
رحلة) قابلة للتجاوز — الرحلة سجلّ لما حدث فعلاً لا لما هو مُسجَّل في الملفات.

🔑 **مبدأ متكرر في الاثنين**: الرحلة تحفظ `branch_code` و`vehicle_type_label` **نصاً**
بجانب الرابط. فلو حُذف الفرع أو غُيّر نوع السيارة لاحقاً، تبقى الرحلة شاهدة على ما
نُفِّذت به.

النِّسب المئوية للفروع وتقرير الفروع حُذفا بطلب المستخدم.

### د) البوليصة وتقرير الرحلات (0023)

بوليصة برقم مميز **ثابت**: تُصدَر مرة واحدة لكل رحلة (`unique` على `trip_id`)، فإعادة
الطباعة تعطي نفس الرقم — البوليصة وثيقة لا صفحة عرض.

🔒 **البوليصة خالية من أي مبلغ إطلاقاً** — لا سعر ولا ترب ولا ديزل ولا ربح. تخرج مع
الشحنة وقد تصل للعميل أو لأي جهة على الطريق، والأعمدة المالية **لا تُطلب من قاعدة
البيانات من الأساس** في صفحتها (قاعدتا #3 و#11).

وتقرير الرحلات: قائمة مفلترة (فترة/سائق/عميل/حالة) بإجماليات البنود وتصدير CSV.

### هـ) ما لم يُختبر
`npm run build` نجح في كل مرحلة و TypeScript يمرّ. **لا شيء جُرِّب في متصفح**، ولا توجد
اختبارات آلية.
