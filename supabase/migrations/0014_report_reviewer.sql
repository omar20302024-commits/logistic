-- ============================================================================
-- 0014 — اسم المراجِع في تذييل كل التقارير
-- ============================================================================
--
-- الاسم إعداد في قاعدة البيانات مش قيمة ثابتة في الكود، لسببين:
--   1) لو المراجِع اتغيّر، ده تعديل من شاشة الإعدادات مش نشر كود جديد.
--   2) اسم شخص متكرر في 13 ملف واجهة بيبقى مستحيل تغييره بأمان.
--
-- فاضي = التذييل ما يظهرش أصلاً.
-- ----------------------------------------------------------------------------

alter table settings
  add column if not exists reviewed_by text not null default 'م/محمود جعفر';
